---
    # Install Ansible roles for GlusterFS
    # ansible-galaxy install -r requirements.yml
    
    # Download gluster.infra from git
    - name: gluster.infra
      src: https://github.com/gluster/gluster-ansible-infra
      version: v1.0.4-20
